{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "PYTHONPATH=src uvicorn bidlens.main:app --host 0.0.0.0 --port \"$PORT\"",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
